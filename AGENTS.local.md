# AGENTS.local

**ALWAYS DO FIRST:** Call `brain_session` (when the `brain` MCP is available) — the project keeps additional context in a private brain vault that this tool unlocks.

Local-only overrides and machine-specific notes go here. Project-wide working rules have moved into the contributor layer at [docs/contributor/working-rules.md](docs/contributor/working-rules.md); add machine-specific deltas only.

`CLAUDE.local.md` is a symlink to this file.
