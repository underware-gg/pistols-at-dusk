# Floor enclosing formations

![Floor enclosing formations](../images/floor_enclosing_formations.png)

## Bounds
- Current reviewed slice: `0,22 -> 7,27`
- Immediate focus: the first two rows of collections in this slice, plus the remaining artefacts to the right on the same `4..6` band of the source-sheet block

## Current Read
- Floor-placed enclosing formations intended to sit on the ground rather than wall-mounted or freestanding furniture.

## Feedback
- Interpretation: These, and the remaining artefacts to the right of them on the same two collection rows, are enclosing formations designed to appear on the floor.
- Composition / usage notes: These should be treated as separate floor-feature collections, not one broad grouped family. First row: yellow / natural stone, with `2` collections total - one `3x3` and one `2x2`. Second row: blue / man-made stone or brickwork, with `5` collections total - `3` separate `3x3` collections followed by `2` separate `2x2` collections. General rule: `2x2` collections are corner-only and non-extensible. A `3x3` collection is extensible because its middle connectors can be repeated.
- Missing related tiles: The immediately following right-hand slice continues the blue / man-made stonework row and contains the final `3x3` plus the two `2x2` collections already counted in the `5`-collection total for that row. Continue reviewing further artefacts to the right only once this brickwork family ends.
- Rename suggestions:
- Action later (do not change yet): Author these as distinct floor-feature collections/kits rather than generic decorative tiles, and keep the natural-stone and brickwork variants related but separate by collection count and footprint.
