# Wordmark and symbol row

![Wordmark and symbol row](../images/ui_wordmark_and_symbols.png)

## Bounds
- Source-sheet cells: `55,20 -> 70,21`
- Tile span: `16 x 2`

## Current Read
- Mixed UI connector and symbol strip containing both `1x1` and `2x2` entities. This is not just branding or decoration; it includes dotted and solid corner/connectivity elements that can contribute to UI composition.

## Seen In References
- `reference-overworld-island-title-screen.png`

## Feedback
- Interpretation: This is another collection of UI elements, mixing `1x1` and `2x2` pieces. Reading left to right, top to bottom from the far left: `1.` a circle with a `+` in it and a circle, `2.` a vertical dotted line and a vertical line, `3.` a horizontal line and a horizontal dotted line, `4 & 5.` a dotted `2x2` square, `6.` dotted T-intersections with double connection on the X axis (top has lower Y connection, bottom has upper Y connection), `7.` dotted T-intersections with double connection on the Y axis plus single left/right connectors, `8.` solid T-intersections with double connector on X, `9.` solid T-intersections with double connector on Y, `10.` a solid four-way connector and a dotted four-way connector, `11 & 12.` a solid `2x2` square.
- Missing related tiles:
- Composition / usage notes: This strip should be treated as a flexible UI construction set rather than a single branding row. Some elements are standalone `1x1` connectors/symbols while others are `2x2` square entities. Review and author them on the grid.
- Rename suggestions:
- Action later (do not change yet): Split this broad strip into exact grid-aligned review units so the `1x1` and `2x2` entities can be reviewed and authored separately. There is also a missed section above, to the right of the checkbox tiles, that should be pulled out as its own grid-aligned review unit.
